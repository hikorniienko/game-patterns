export class TypeObject {
  update = (): void => {
    console.error('TypeObject.update() must be overridden')
  }

  render = (): void => {
    console.error('TypeObject.update() must be overridden')
  }
}
